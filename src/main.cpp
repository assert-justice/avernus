#include <iostream>
#include "engine/engine.hpp"
#include "game/game.hpp"

int main(){
    Game game;
    // Engine engine;
    // engine.setup = app_setup;
    // engine.start = app_start;
    // engine.update = app_update;
    // engine.draw = app_draw;
    // engine.init();
    // if(engine.hasError()) return 1;
    std::cout << "hello" << std::endl;
    return 0;
}